name: authentication_repository
description: Dart package which manages the authentication domain.
version: 1.0.0

environment:
  sdk: '>=2.6.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  equatable: 
  firebase_auth: 
  firebase_core:
  google_sign_in: 
  meta:

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.0.0