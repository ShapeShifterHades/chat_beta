name: authentication_repository
description: Dart package which manages the authentication domain.
version: 1.0.0

environment:
  sdk: '>=2.6.0 <3.0.0'

dependencies:
  flutter: 
    sdk: flutter
  equatable: any
  firebase_auth: any
  firebase_core: any
  google_sign_in: any
  meta: any

dev_dependencies:
  flutter_test:
    sdk: flutter